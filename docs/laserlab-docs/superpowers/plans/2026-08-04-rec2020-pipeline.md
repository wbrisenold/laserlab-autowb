# Rec.2020 Pipeline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Rec.2020 to the DCTL round trip and align project-scoped SDR output settings.

**Architecture:** Append one gamut option and matrix pair without changing existing parameter indices. Apply three guarded project settings after a dry-run preview, leaving global preferences untouched.

**Tech Stack:** DaVinci DCTL, Python assertions, Resolve scripting API.

## Global Constraints

- Preserve all existing input-gamut indices and defaults.
- Do not mutate global Resolve preferences.
- Do not attempt to automate the OFX Input Gamut selection.
- This directory is not a Git repository; commit steps do not apply.

---

### Task 1: Add Rec.2020 DCTL Gamut

**Files:**
- Modify: `test_tone_controls.py`
- Modify: `OKLabGrade-NoDRT.dctl`

- [ ] Add failing assertions for `g_rec2020`, `R10_C0`, `G10_C0`, clamp maximum `10`, and index-10 routing.
- [ ] Run `python3 test_tone_controls.py` and confirm the new assertion fails.
- [ ] Append the Rec.2020 combo value, matrices, and branches.
- [ ] Run `python3 test_tone_controls.py` and the OpenCL syntax command; expect exit code `0`.

### Task 2: Align Project SDR Settings

**Files:** None; project settings only.

- [ ] Dry-run `safe_set_project_settings` for `colorSpaceOutput=Rec.709 Gamma 2.4`, `timelineWorkingLuminance=100`, `timelineWorkingLuminanceMode=SDR 100`, and `useInverseDRT=0`.
- [ ] Apply the same settings and read them back.
- [ ] Refresh the LUT list through Resolve scripting and confirm `True`.
